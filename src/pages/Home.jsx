import React, { useContext } from "react";
import { useSelector } from "react-redux";
import Navbar from "./Navbar";

const Home = () => {
  
  
  return (
    
    <div className="p-6">
      
      
    </div>
  );
};

export default Home;
